import { Button } from 'antd';
import React from 'react';
import styles from '../styles/Buttons.module.scss';

const StrokeButton = () => {
  return (
    <div className={styles.StrokeButton}>
      <Button type='Default' />
    </div>
  );
};

export default StrokeButton;
